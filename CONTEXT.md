# ICT 合规诊断工具 — 数据敏感性与部署上下文

本上下文记录「公网部署 + 数据脱敏」设计中被反复确认的领域语言，避免每次重新推导。
工具本体定位见 `CLAUDE.md`。

## Language

**准标识符四要素**:
客户名称、项目名字、项目金额、项目时间。单独一个泄漏属可控风险；四者（或其关键子集）在服务器上以明文凑齐，即构成可重识别某一笔生意的商业泄密。
_这才是「敏感」的真正定义——是组合，不是单字段。_

**枚举字段**:
诊断规则引擎消费的全部字段（`project_type`、`contract_content_same`、`capability_ratio`…）均为分类/布尔值，非 PII。
_Avoid_: 把金额当数值字段——金额以 `gross_margin` 的分桶枚举（如 `lte_0`）进规则，规则引擎只有 `eq`/`in`/`neq`，无数值比较。

**核算单元**:
一笔合同（=一个 BPM 商机）内被**分别核算**的最小业务块，有**申报业务类型**（设备/施工/服务/标品）、金额、税率、毛利、**是否属集团白名单**（`whitelisted`）。**2026-06 集团 27 号文重构后单元角色（`docs/adr/0004`）**：第一遍项目级判**列收模式**时，单元是**数据采集层**（贡献申报类型/金额/白名单/利润，汇总算占比与整体利润率）；只有项目级走不通退第二遍，单元才是**兜底判定单位**（非白名单/不达标→净额、达标白名单单元仍全额）。硬件/施工**默认净额**（铁律降级为兜底，`enforce_hardware_no_listing` 设默认值），但 `listed` 最终由 `classify_listing_mode` **派生**——非用户填。硬转服务检测仍是单元级（`docs/adr/0002` #9）。残留缺口：服务侧毛利规则仍按项目级单值，未按服务单元逐块跑（见下方「服务侧毛利率」词条）。
_Avoid_: 「列收单元」（预设了列收，装不下不列收的块）、「子项目」（与商机/项目粒度撞车）、「项目模块」（丢财务语义）。

**列收（产数列收）**:
收入是否计入产数业绩、以及按全额还是净额。**2026-06 集团 27 号文推翻旧铁律**：列收方式**不再由申报类型单点推导**，改由**列收模式**级联判定（先项目级套全额，走不通逐单元兜底）。硬件/施工**默认**净额，但**集团白名单**硬件/成品软件在「门槛 + 控制权」齐备时可全额。「净额」从铁律降级为**兜底**。
_Avoid_: 「硬件铁律不列收」作为绝对公理——已被白名单全额场景打破，只剩「默认兜底」语义。

**列收模式（四模式，2026-06 新增）**:
27 号文把列收方式收敛为四种，工具按**级联**选：①常规 ICT（服务成本型/资本模式全额、收支差净额）②服务整合业务模式（全额；签约≥300万、硬件+集成施工占比≤60%、整体利润率≥10%）③单一履约·白名单（全额；场景一含服务>100万占比≤80%利润≥5%，场景二纯硬件≥500万利润≥5%）④单一履约·非白名单/收支差（净额代收代付，利润≥5%）。**级联**：第一遍项目级套全额模式，过线则整个项目（含非白名单硬件）全额；走不通退第二遍逐核算单元，把非白名单/不达标单元划净额、达标白名单单元仍可全额。**选模式靠业务实质路由、不靠门槛排队**（27 号文「按是否形成重大组合产出划分」）：①电信投资设备打包→资本模式 ②[[重大整合]]→服务整合 ③标准白名单+单独履约→单一履约·白名单 ④都不是→收支差净额；门槛（60%/80%/利润率）是**进路后的资格闸**，不是选路依据。
_Avoid_: 把模式判定下放到单元级首判（首判是**项目级**，单元级只是兜底 + 喂数据）；用门槛高低「排队试」选模式（错——实质先路由）。

**两套占比公式（逐字核对 27 号文 P11/P12，2026-06 钉死）**:
两个占比长得像、口径全不同，分类器 `classify_listing_mode` 的正确性全压在这——
- **服务整合 ≤60%**（P11 门槛3）：`(设备 + 施工) / 项目整体`。集成**施工**跟硬件一起进**分子**（当料工），分母是项目整体收入（含一切）。卡的是「料工别太多」。
- **单一履约·场景一 ≤80%**（P12，含服务项目）：`(设备 + 标品) / (设备 + 标品 + 服务)`，服务 = ICT服务收入＋基础业务收入＋三自收入（自交付/自运维/自运营）。分母**逐项列举、不含集成施工**——故 `declared_type=施工` 的单元金额在单一履约占比里**整个排除**（既非分子也非服务），后果是分母变小、占比被推高、更难过线（**偏严**，留举证、不硬前置禁止）。验算锚点：P13 案例 2057/2592.6=79.3%（分母=硬件+集成服务+三自）。
- **双口径不强行统一**（27 号文自带、非矛盾）：「基础业务收入」**进**单一履约占比**分母**，但利润率口径明令「剔除非项目型 ICT 收支（云/小微/基础）」**不进**利润率。报告对两个数各自注明口径来源。
_Avoid_: 把「集成施工」放进单一履约公式讨论（伪命题——单一履约只认「集成**服务**=ICT服务收入」进分母，没有施工概念）；为了对齐而把两套口径捏成一套。

**全额准入闸（硬/软分治，2026-06 钉死）**:
占比/利润率之外的全额资格前置，分两档——**硬否决**（不满足→直接落净额兜底、红字可举证）：客户类型（不在 党政军/央国企/事业/互联网头部 闭集）、付款节点（非「首付款+到货验收尾款」）；**软提示**（黄字需举证、仍算建议模式不踢出）：产权转移（验收后才转移、时点常未到）、集采比例≥60%（省授权市采购待定）。**客户类型硬否决只对「明确闭集外」（民营中小/个人）生效**，「互联网头部/5A 外企」这类无硬名单靠人判的，**退软让用户填**。
_Avoid_: 把「软」理解成对闭集准入放水——软只给「政策还在动」（集采省授权待定）和「时点未到」（产权在途）的条款，白纸黑字的闭集准入（客户类型/付款节点）该硬就硬。

**重大整合（单一组合产出）**:
区分 服务整合 vs 单一履约 的分水岭实质信号：硬件与服务**深度耦合、电信做了重大修改/定制、交付的是一个功能完整的单一系统**（而非分别提供的商品+服务）。采集对齐控制权角色：AI **只在用户明确说「深度定制/重大修改/系统级集成」才抽**，否则面板让用户自勾 + 举证，**绝不臆测**。
_Avoid_: 拿「提供了集成服务」这种笼统话当重大整合（驳回案例第 14 页正是栽在这——「统筹交付的集成能力」太笼统、无可验证交付物）。

**集团白名单**:
27 号文附的可全额列收的标准化**硬件设备/成品软件**目录（计算存储/网络/安全/无线/终端/AI 机器人/机房配套/低空经济 + 基础/通用/行业/安全软件）。是「全额资格」的**入参/门票**（进占比、进准入），不是逐元抠分依据。室分/三线/综合布线/弱电=施工本质，不进白名单全额；算力设备走资本投资全额、暂不开单一履约。
**建模**：目录做成版本化引用数据 `whitelist.json`（与 rules/clauses 并列、改不动码），**大类粒度**（型号级不可维护）。核算单元加 `whitelisted` 三态字段，**只对 `declared_type ∈ {设备, 标品}` 有意义**（施工恒非白名单、服务不适用）；AI 往大类靠**提议** in/out，但 true 是全额门票、高风险，**必须用户面板确认**，AI 拿不准默认 `unknown`。`whitelisted=unknown` **保守当非白名单**进占比（偏严待举证，不放水），报告黄字提示确认后或可改善。目录宽进——诊断价值在抓**落在白名单外的例外**（LED 屏/机房装修/室分弱电/非标件）。
_Avoid_: 把白名单当成「白名单硬件就一定全额」——它只是必要入参，仍需项目级门槛 + 控制权。

**服务侧毛利率（`gross_margin`）**:
**单元级**测量「这块服务是不是真服务」。语义是「**应列收（服务）侧的毛利**」，**绝不混入设备/施工的毛利**：把平进平出的硬件块混算进来会误触发 R03（三零）/R12（过手）/R32（服务混合低毛利）及硬转服务。**只**喂三零/过手/硬转服务类检测。
_Avoid_: 和下面的项目整体利润率混为一谈——两者算法、口径、用途全不同。

**项目整体利润率（`overall_margin`，2026-06 新增）**:
**项目级**测量「够不够全额列收资格」。算法 = 项目整体收入−成本（**含硬件**）；口径按新政「税前利润率、剔除非项目型 ICT 收支（云/小微/基础）」，由**用户填已剔除值**，工具给口径说明。毛利/利润口径**不较真**、报告注明「按填报口径估算」。复用 `gross_margin` 同套分桶枚举（切点正好卡 5%/10%）。**只**喂列收模式分类门槛（服务整合≥10%/单一履约≥5%），**严禁**喂三零/过手检测（否则硬件污染回归）。
_Avoid_: 旧 CONTEXT 的绝对禁令「不存在项目整体毛利率」——新政合法需要它，但用途被严格隔离在模式分类。

**硬转服务**:
一个**申报为「服务」（以求列收）的核算单元，实质却是硬件/施工**——申报类型与实质背离。这是诊断要抓的**核心包装风险**。R24/R25 本就为此命名；2026-06-05 起由 `detect_hard_to_service` 按服务单元做**举证式**检测（三信号：零毛利平进平出 / 物流供应商直发 / 无自有能力，旁证数量定嫌疑等级，不自动定性）。残留盲区：申报为「标品/其他」的单元绕过检测（标品马甲，见 [[project-diagnosis-logic-issues]]）。

**控制权资格（总额法资格）**:
**项目级**判断——电信对**整个项目**是否拥有控制权，决定能否按总额法列收（主要责任人）还是只能净额/代收代付（代理人）。判定操作化为省公司《产数ICT业务高质量发展专项部署材料》（2026-06-03）的「**关键角色矩阵**」：六到位全流程 19 个角色中 **10 个进判定**——**必选**电信全占「6 应标签约统筹 / 7 软硬件采购决策 / 9 全流程交付管理」（**+16 设备管理，仅涉硬件时必选**）；**三组二选一**各占一个：方案{3 设计者|4 整合确定者}、交付实施{10 设计者|11 确定责任者}、实施开发{13 实施者|14 主导责任者}。必选全占 + 三组各≥1 → 资格成立。三组二选一的全排列即「**8 种情形**」（2×2×2）。设计决策见 `docs/adr/0003`（与会计要件六维度融合并立，举证式高风险定级；**已实现**，2026-06-09 上线，引擎入口 `assess_control_roles`）。
_Avoid_: 「7 个角色」——不严谨简写（单一情形占 6-7 个，但参选角色共 10 个，字段应给全 10 个）；把 19 角色全纳入判定（另 9 个为配合/客情/售后角色，不参选）。

**关键角色 / 配合角色**:
进总额法判定的那 10 个为「**关键角色**」（决策 / 主导 / 责任）；其余 9 个（客情对接、采购执行配合、交付/实施/设备/运维配合者）为「**配合角色**」，不证明控制权、不进判定。电信只占配合角色 = 过手嫌疑。

**敏感自由文本**:
用户在对话中输入的原文。准标识符四要素只活在这里，不进结构化字段（唯一例外是 `bpm_id`，可正则脱敏）。脱敏的真正目标对象就是这段文本。

**内网版 / 公网版**:
两个平行部署变体。内网版数据不出公司（含本地大模型替代 DeepSeek），功能完整、含跨人复核；公网版受脱敏边界约束、强制前端脱敏、砍掉跨人复核、退化为「前线人员单人自助诊断」。
_Avoid_: 把两者做成两份 fork 代码——应是同一套代码 + `DEPLOYMENT_MODE` 开关。

**脱敏边界 (ii)+(iii)**:
(ii) 明文 PII 不得落在任何公网可达服务器上（含自有服务器）；(iii) 全程 HTTPS。第三方 DeepSeek 这一跳同样在约束内。

## Relationships

- 诊断引擎只依赖**枚举字段**，与**准标识符四要素**正交 → 脱敏四要素不损伤诊断结论（已由 `rules.json` + 字段定义证实）。
- **毛利率（应列收/服务侧）** 与**列收**同源：都只对服务单元有意义 → 毛利驱动的规则（R03/R12/R32）本质上是在问「服务侧列收风险」，扁平 `gross_margin` 必须只承载服务侧毛利。
- **公网版** 受 **脱敏边界 (ii)+(iii)** 约束 → 倒逼「前端脱敏」（方案 A）。
- 客户端脱敏对**自由文本**只能「尽力」（NER 非 100%）；只有**确定性手段**（专属输入框 + 正则）能提供「保证不漏」。
- **控制权资格**（项目级，关键角色矩阵）与**硬转服务**（单元级，服务单元实质）是「**同一根问题——控制权——的两个尺度**」：项目级判电信是否主导整个项目（外层闸门），单元级判某服务单元是真服务还是包装硬件（内部明细）。**互补覆盖、不冲突**：项目级抓「连项目都不主导」（单元级看不全），单元级抓「项目整体受控但某块仍是包装」。故不是「并存/替换」之争，而是分层各管一段。
- **控制权资格**（闸门）→ **列收模式**（选哪种全额/净额）→ **差口举证**（缺啥补啥）三层串联：控制权是所有全额模式的总前提，模式分类坐在它下游，工具只走到「分类 + 举证」（①②③），**不**下财税科目（④ 只作报告附注）。
- **项目整体利润率** 与 **服务侧毛利率** 同源不同用：前者含硬件、喂模式门槛；后者只服务侧、喂三零/过手。两者切点共用同套分桶枚举，但**严禁互串**。

## Flagged ambiguities

- 「脱敏边界」最初被理解为「浏览器→服务器」单跳；澄清后实为 (ii)+(iii)，且 DeepSeek 第三方跳同样在内——现状（内网调外部 DeepSeek）其实已不合规，靠「测试阶段」容忍。
- 「风险」最初按逐字段（单条泄漏即违规）理解；澄清后为**组合可重识别性**——准标识符四要素凑齐才算泄密，单字段可控。
- 「前端脱敏」最初设想为浏览器自动识别 PII；澄清后，自由文本里的机构名/项目名无法靠 NER 保证，需用**专属字段**做确定性脱敏。
- 用户口语「项目分三层」混淆了两类东西：设备/服务是可分别核算的**核算单元**，而「自有能力」是跨单元的**属性**（对应 `has_telecom_capability`），不是一层。canonical 概念定为**核算单元**，「列收单元」因预设列收被否决。
- **毛利率最初被当作「项目整体毛利率」**（单一扁平值喂 R03/R12/R32）；澄清后实为「应列收/服务侧毛利」——列收已按核算单元免疫硬件/施工，毛利这条线却没跟上。现状修法（2026-06-06）：(i) 收紧扁平字段语义为「服务侧」+ AI 抽取提示禁止混入硬件毛利（已做）；(ii) 规则按服务单元逐块跑毛利（**已知缺口**，需引擎支持逐单元规则迭代，类比硬转服务检测，未实现）。
- **2026-06 集团 27 号文（硬件列收）重构已实现**（决策落 `docs/adr/0004`，规则库 v1.8.0）：硬件铁律→默认兜底；列收模式四分类 + 项目级优先/单元级兜底级联（`classify_listing_mode`）；新增 `overall_margin`；工具止于「分类+举证」不下科目；模式选择 = 业务实质路由器 + 「重大整合」分水岭；白名单 = `whitelist.json` 大类 + 单元 `whitelisted` 三态；两套占比公式 + 双口径已逐字核对钉死（见上「两套占比公式」词条）；时点法 v1 轻做（默认全算非周期 + 报告口径提示，未加 `recurring`）；存量规则对齐（R24/R25 让位 #9、R26 升格物权否决闸、R21/R22/R23 reframe 并立）。
**两个 60% 别混**（grill 中澄清）：①**服务整合**「硬件+集成施工金额占比≤60%」（第 11 页，资格闸·上限，进占比公式）②**单一履约**「集采比例≥60%」（第 12 页，后向采购里走集采框架协议的设备占比，**软建议**·下限，且「省采购是否授权市采购待定」→ 单列字段、缺则黄字提示不一票否决）。
**仍开放（下次从这接）**：算力/室分弱电 carve-out 的类型级硬规则（当前靠 `whitelisted` 三态人工标，未做类型级硬排除）；第二遍单元兜底「达标」判定借用项目级门槛，27 号文未细化单元级门槛——待真实案例校准；listing_mode 是否计入 overall_risk（当前纯分类不抬风险，「奔全额但不合格」的风险信号待接）。
