# ICT 合规诊断工具 — 数据敏感性与部署上下文

本上下文记录「公网部署 + 数据脱敏」设计中被反复确认的领域语言，避免每次重新推导。
工具本体定位见 `CLAUDE.md`。

## Language

**准标识符四要素**:
客户名称、项目名字、项目金额、项目时间。单独一个泄漏属可控风险；四者（或其关键子集）在服务器上以明文凑齐，即构成可重识别某一笔生意的商业泄密。
_这才是「敏感」的真正定义——是组合，不是单字段。_

**枚举字段**:
诊断规则引擎消费的全部字段（`project_type`、`contract_content_same`、`capability_ratio`…）均为分类/布尔值，非 PII。
_Avoid_: 把金额当数值字段——金额以 `gross_margin` 的分桶枚举（如 `lte_0`）进规则，规则引擎只有 `eq`/`in`/`neq`，无数值比较。

**核算单元**:
一笔合同（=一个 BPM 商机）内被**分别核算**的最小业务块，有**申报业务类型**（设备/成品软件/施工/服务/标品/其他）、金额、税率、毛利、**是否属集团白名单**（`whitelisted`）。核算单元确认是**结构与事实采集层**：确认合同里有什么、各块金额及属性，不在这一层判断控制权，也不直接决定全额或净额。结构确认后，系统为每组候选单元派生组合或分别关系；同一项目可以部分组合、部分独立，不再用一个项目级 `performance_obligation_mode` 覆盖全部单元。每个非标品最终核算单元由用户确认 `listing_intent`，系统通过后续判断链派生只读的 `listing_result`；标品始终全额。硬转服务检测仍是单元级（`docs/adr/0002` #9）。残留缺口：服务侧毛利规则仍按项目级单值，未按服务单元逐块跑（见下方「服务侧毛利率」词条）。
**用户界面术语**：客户经理只维护和查看「核算单元」，不再同时维护一套名为「履约义务」的对象。系统内部仍保留履约义务的组合关系：分别履约时，原始核算单元与最终列收判断单位一一对应；组合履约时，多个原始核算单元归入同一个组合判断组，并在界面上仍以组合后的「核算单元」呈现。组合只改变判断边界，不得抹平组内各构成项的类型、金额和白名单等事实。
_Avoid_: 「列收单元」（预设了列收，装不下不列收的块）、「子项目」（与商机/项目粒度撞车）、「项目模块」（丢财务语义）。

**列收（产数列收）**:
项目语境中的口头简称：「列收」专指**全额列收**，「不列收」专指**净额列收**，并非完全不计收入。所有已提交核算单元的测算方式只有全额或净额两种，不把「不确定」建成第三种金额口径。除恒全额的标品外，用户先对每个最终核算单元确认拟全额或拟净额；拟全额才进入六到位、R08 控制权和省内配套门槛。条件明确不满足时最终净额；仅缺证据或资格未知时，自检仍按拟全额测算，但结果标为暂定并计高风险。
_Avoid_: 在界面和报告中只写「列收 / 不列收」而不写全称；把「不列收」解释成完全不计收入；把「硬件铁律不列收」作为绝对公理。

**核算单元列收意图**:
每个非标品最终核算单元拟采用的列收方式，统一存为 `listing_intent`，只取 `full`（拟全额列收）或 `net`（拟净额列收）二态；未选择是表单未完成状态，不是第三种业务值。AI 只在对话明确出现「全额列收 / 列收 / 净额列收 / 不列收 / 代收代付」等意图时预填，没有依据则留空、不猜默认值；用户提交确认面板即视为接受或修改当前选项，人工确认值是意图事实来源。标品恒全额，不显示可编辑意图；「其他」不得进入最终提交。
**关系**：`listing_intent=net` 直接得到 `listing_result=net`，并跳过该单元的六到位、R08 及全额资格检查；`listing_intent=full` 才进入完整判断链。全部条件明确通过时得到确认全额；任一条件明确不满足时得到确认净额；仅存在待补证据、白名单未知等未决项时，仍得到暂定全额测算并计高风险。服务单元只有拟全额时才执行硬转服务检测。
**最终列收结果**：`listing_result` 是供报告和后续规则统一消费的只读二态——`full`（全额列收）或 `net`（净额列收），不得由 AI 或用户直接填写；另用 `listing_result_status=confirmed|provisional` 区分确认结果与暂定测算。暂定不是第三种金额口径：`listing_result=full + listing_result_status=provisional` 表示「当前按全额测算，但全额资格尚未证实」。标品恒为确认全额；其余单元按「列收意图 + 已知条件 + 未决证据」派生。旧 `listed` 仅用于读取历史数据，新诊断不再依赖。
**整体风险汇总**：项目 `overall_risk` 取规则风险与各核算单元风险的最高等级，但只汇总风险提示，不改写其他单元的 `listing_result`。任一拟全额单元存在暂定资格、待补证据、白名单未知、硬转服务嫌疑，或因明确不满足全额条件而最终落净额，均把项目整体风险抬为高；用户一开始即选择拟净额的单元跳过全额资格检查，不因这些被跳过条件增加风险。
**边界**：真实服务通常全额列收，只有利润率特别低等不达标情形才会净额列收，实际占比极小；硬转服务的目的恰是争取全额列收，因此通常表现为「申报服务 + 确认全额 + 呈现硬件/施工实质」，而不是主动选择净额。
**低利润处理**：利润率特别低只触发提示或风险，不得由系统自动把服务改成净额列收；最终列收意图仍由用户确认。
_Avoid_: 使用 `not_listed` 第三态，或把「待确认」建模成列收意图；把用户的拟全额意图直接当成最终全额结果；要求标品重复选择列收意图。

**标品**:
电话、宽带、天翼云等电信自有标准化产品，始终按全额列收，不属于 27 号文白名单判定对象。
_Avoid_: 成品软件、白名单软件、外购软件。

**成品软件**:
Oracle、Windows 等从第三方采购的标准软件或授权，与硬件一起接受 27 号文白名单、金额门槛和控制权判断。
_Avoid_: 标品、电信自有产品、定制软件开发。

**关系**：**标品**与**成品软件**是互斥概念；前者恒全额，后者与硬件同属 27 号文判定对象。

**其他（核算单元草稿类型）**:
AI 切分阶段暂时无法归类的占位类型，不是合法的最终申报类型；提交诊断前必须由用户归入设备、成品软件、施工、服务或标品。
_Avoid_: 让「其他」进入最终诊断并稀释 60%/80% 占比。

**列收模式（集团规范 + 省内配套自检口径）**:
硬件列收分类器只在项目存在**设备、成品软件或施工**核算单元时启动；纯服务、纯标品、标品+服务项目不运行该分类器，结果标记为 `not_applicable`，报告不显示「列收模式判定」板块，标品与服务的结果直接进入核算单元明细。集团 27 号文和《企业会计准则第 14 号——收入》提供履约义务拆分、控制权和清单范围；签约额、60%/80% 占比、5%/10% 利润率及 500 万等数字来自另行保留的**省内配套自检口径**，不得标成「27 号文硬条件」，原始依据待补。分类器不再采用「项目级全额失败后逐单元兜底」的级联模型，也不要求整个项目只能选一个分支；它对每组候选关系派生：①多个承诺形成组合产出时，生成一个组合核算单元并整体判断控制权；②承诺可明确区分时，保留为独立核算单元并分别判断控制权。同一 BPM 可以同时包含两类结果。省内配套口径继续生效：组合核算单元对应服务整合门槛，独立核算单元对应原省内材料所称「单一履约」的含服务场景或纯硬件/成品软件场景；项目级占比和利润率仍按整个 BPM 取数。电信投资设备打包的资本模式仍单独留出口。
**两层结论**：第一层按会计准则和集团 27 号文，以履约义务拆分结果 + 控制权判断是否具备总额法基础；第二层再套省内金额/占比/利润率等产数自检门槛，决定最终 `listing_result=full|net`。任一层未通过，最终产数结果均落净额；报告必须标明是「会计总额法基础不成立」还是「会计基础成立但省内产数门槛未过」，不得把后者写成违反 27 号文或会计准则。
_Avoid_: 把分别履约称为「单一履约兜底」；不做履约义务拆分就直接项目级一票决定全部单元；强迫一个项目只能全部组合或全部分别；用门槛高低「排队试」选模式（错——实质先路由）。

**省内配套自检的两套占比公式（原始依据待补）**:
两个占比长得像、口径全不同，分类器 `classify_listing_mode` 的正确性全压在这——
- **服务整合 ≤60%**（P11 门槛3）：`(设备 + 成品软件 + 集成施工) / 项目整体`。分子和分母均按**整个 BPM 项目**汇总，即使项目中还存在未归入该组合判断组的独立核算单元，也不把它们从项目整体口径中剔除。成品软件与设备同属外购标准化商品，集成**施工**也进**分子**（三者均作料工）；标品只进入项目整体**分母**，不进分子。卡的是「料工别太多」。
- **分别履约·含服务场景 ≤80%**（省内材料原称「单一履约·场景一」，P12）：`(设备 + 成品软件) / (设备 + 成品软件 + 服务 + 标品)`。其中服务承载 ICT 服务/三自收入，标品承载电话、宽带、天翼云等电信自有产品收入；标品只进分母，不能冒充成品软件进入分子。分母**不含集成施工**——故 `declared_type=施工` 单元金额在该占比里**整个排除**（既非分子也非服务），后果是分母变小、占比被推高、更难过线（**偏严**，留举证、不硬前置禁止）。验算锚点：P13 案例 2057/2592.6=79.3%（分母=硬件+成品软件+集成服务+基础业务+三自）。
- **双口径不强行统一**：「基础业务收入」**进**分别履约含服务场景的占比**分母**，但配套利润率口径要求「剔除非项目型 ICT 收支（云/小微/基础）」**不进**利润率。报告对两个数各自注明口径来源。
_Avoid_: 把「集成施工」放进分别履约含服务场景的公式讨论（该省内公式只认「集成**服务**=ICT服务收入」进分母，没有施工概念）；为了对齐而把两套口径捏成一套。

**全额准入闸（硬/软分治，2026-06 钉死）**:
占比/利润率之外的全额资格前置，分两档——**硬否决**（不满足→直接落净额兜底、红字可举证）：客户类型（不在 党政军/央国企/事业/互联网头部 闭集）、付款节点（非「首付款+到货验收尾款」）；**软提示**（黄字需举证、仍算建议模式不踢出）：产权转移（验收后才转移、时点常未到）、集采比例≥60%（省授权市采购待定）。**客户类型硬否决只对「明确闭集外」（民营中小/个人）生效**，「互联网头部/5A 外企」这类无硬名单靠人判的，**退软让用户填**。
_Avoid_: 把「软」理解成对闭集准入放水——软只给「政策还在动」（集采省授权待定）和「时点未到」（产权在途）的条款，白纸黑字的闭集准入（客户类型/付款节点）该硬就硬。

**履约义务拆分结果**:
按《企业会计准则第 14 号——收入》第十条及集团 27 号文，对**每组候选组合关系**派生的二态结论：`combined`（该组承诺不可明确区分，生成一个组合核算单元）或 `separate`（该组承诺可明确区分，保留为多个独立核算单元）。同一 BPM 可以同时存在若干组合核算单元和若干独立核算单元；项目本身不再有一个覆盖全部单元的组合/分别二态结论，`mixed` 如需存在也只能是报告摘要，不能作为规则输入。
**交互边界**：履约义务是系统内部的会计判断关系，不作为第二套用户对象展示。页面和报告统一称「核算单元」；组合/分别关系只决定候选单元最终是合并判断还是分别判断。每个原始核算单元必须且只能归属一个最终核算单元，不能遗漏或重复。
**组合确认流程**：采用「AI 预组 → 用户调整 → 规则推导 → 用户确认」。AI 根据合同描述生成候选组合草稿；用户可以勾选原始核算单元执行组合，也可以拆分已建议的组合；每个候选组合分别填写 PO1–PO4，系统据此推导组合或分别建议，最终以用户确认的核算关系为准。不得要求用户从空白状态手工搭建全部组合，也不得让 AI 建议绕过人工确认直接成为最终关系。
**变更后重评**：原始单元类型、组成或组合关系发生变化时，所有受影响的最终核算单元必须重新生成并重新评估。原 `listing_intent`、六到位和 R08 人工答案可以保留为界面草稿供参考，但立即失去有效确认状态，不得继续参与最终结果，必须由用户重新确认。核算单元变化不得清空或要求重填关键角色、交付模式等项目级共性事实；事实本身未变时继续复用并重新生成单元建议。反向地，只要任何项目级共性事实被修改，所有拟全额单元的六到位和 R08 均进入「需重新确认」。类型或金额变化还须按整个 BPM 重新计算 60%/80%、项目整体利润率等项目级门槛；因此未改动单元的六到位/R08无需重填，但其最终列收结果仍可能因项目级门槛变化而自动更新。
**拆分事实 `PO1–PO4`**：针对每组候选组合分别确认：`PO1` 客户能否从其中设备/成品软件本身或结合易于取得的资源中独立受益；`PO2` 电信是否提供重大服务，把组内各部分整合成客户购买的组合产出；`PO3` 是否对组内其他承诺作重大修改或定制；`PO4` 组内各部分是否高度关联、无法独立交付验收。`PO1=yes` 且 `PO2–PO4` 全否 → `separate`；`PO1=no` 或 `PO2–PO4` 任一为是 → `combined`。四项在草稿阶段可未填，但正式确认该组关系前必须明确是/否。
_Avoid_: 把「提供了集成服务」这种笼统表述直接等同 `combined`；让用户凭感觉选择「重大整合」结论；全项目只回答一套 PO1–PO4；把未填写建模成第三种业务结果。

**集团白名单**:
27 号文附的可全额列收的标准化**硬件设备/成品软件**目录（计算存储/网络/安全/无线/终端/AI 机器人/机房配套/低空经济 + 基础/通用/行业/安全软件）。对设备和成品软件是「全额资格」的**入参/门票**：组合履约时作为组合项目的构成信息；分别履约时归属于对应设备/成品软件履约义务，逐项参与最终判断。算力、室分、三线、综合布线、弱电等名称不得直接锁死类型，必须按业务实质拆分：电信自投资的算力设备进入资本模式；向客户独立交付的服务器、存储等仍可作为普通设备判断白名单；布线、安装、施工部分归施工，单独列明并独立交付的标准设备可另拆设备核算单元。标品始终全额列收，不受白名单资格闸影响，也不记录白名单归属。
**建模**：目录做成版本化引用数据 `whitelist.json`（与 rules/clauses 并列、改不动码），**大类粒度**（型号级不可维护）。设备和成品软件单元的 `whitelisted` 为三态字段；AI 往大类靠**提议** in/out，但 true 是全额门票、高风险，**必须用户面板确认**，AI 拿不准默认 `unknown`。自检阶段允许保留 `unknown`，它表示「白名单资格尚未举证」而不是「否」；拟全额单元仍按全额测算，结果标为暂定并计高风险，报告逐项提示正式送审前必须确认是/否并补充依据。明确为 `false` 才作为已知不满足条件处理。目录宽进——诊断价值在抓**落在白名单外的例外**（LED 屏/机房装修/室分弱电/非标件）。
_Avoid_: 把白名单当成「白名单设备/成品软件就一定全额」——它只是必要入参，仍需对应履约义务控制权 + 省内配套门槛；把标品放进白名单判定，或用白名单结果把标品判成净额。

**服务侧毛利率（`gross_margin`）**:
**单元级**测量「这块服务是不是真服务」。语义是「**应列收（服务）侧的毛利**」，**绝不混入设备/施工的毛利**：把平进平出的硬件块混算进来会误触发 R03（三零）/R12（过手）/R32（服务混合低毛利）及硬转服务。**只**喂三零/过手/硬转服务类检测。
_Avoid_: 和下面的项目整体利润率混为一谈——两者算法、口径、用途全不同。

**项目整体利润率（`overall_margin`，2026-06 新增）**:
**项目级**测量「够不够全额列收资格」，范围固定为整个 BPM 项目，不因履约义务组合或分别判断而改成单元级利润率。算法 = 项目整体收入−成本（**含硬件**）；口径按省内配套自检要求「税前利润率、剔除非项目型 ICT 收支（云/小微/基础）」，由**用户填已剔除值**，工具给口径说明。毛利/利润口径**不较真**、报告注明「按填报口径估算」。复用 `gross_margin` 同套分桶枚举（切点正好卡 5%/10%）。**只**喂省内配套门槛（服务整合≥10%/分别履约原单一履约场景≥5%），**严禁**喂三零/过手检测（否则硬件污染回归）。
_Avoid_: 旧 CONTEXT 的绝对禁令「不存在项目整体毛利率」——新政合法需要它，但用途被严格隔离在模式分类。

**硬转服务**:
一个**申报为「服务」（以求列收）的核算单元，实质却是硬件/施工**——申报类型与实质背离。这是诊断要抓的**核心包装风险**。R24/R25 本就为此命名；2026-06-05 起由 `detect_hard_to_service` 按服务单元做**举证式**检测（三信号：零毛利平进平出 / 物流供应商直发 / 无自有能力，旁证数量定嫌疑等级，不自动定性）。原始服务单元被纳入组合核算单元后，只要该组合单元拟全额，仍对组内原始服务明细逐项运行检测；组合关系不能屏蔽误申报风险。命中只标高风险并生成标准证据清单，不自动修改原始类型、拆散组合或改成净额；若后续确认实质为硬件/施工，由用户修改原始类型并触发组合、占比和结果重算。残留盲区：申报为「标品/其他」的单元绕过检测（标品马甲，见 [[project-diagnosis-logic-issues]]）。

**六到位自查（统一模块）**:
在一个项目中只采集一次项目角色、交付模式等**共性事实**，但六维逐项结论与强/中/无综合结论必须落到每个准备全额列收的**最终核算单元**。组合履约形成的组合核算单元确认一次；分别履约时，各个准备全额列收的独立核算单元分别确认；已经选择净额的单元跳过。确认顺序固定为：**项目级交付模式与关键角色事实 → 复用到目标核算单元 → 该单元六个维度逐项确认 → 该单元强/中/无综合结论确认**。前一层是共享客观事实，后一层是单元结论，二者在一个模块内衔接但不能混成同一个字段。
**不存在项目级六到位结论**：系统不得汇总出「整个项目强/中/无」或用某个项目级六到位状态一票改变所有单元。项目中没有任何单元准备全额列收时，六到位模块不显示、不要求填写；净额单元既不参加六到位，也不因六到位证据缺失增加风险。所谓项目级信息只是一份可复用的事实来源，不是项目级资格结论。

项目级角色事实操作化为省公司《产数ICT业务高质量发展专项部署材料》（2026-06-03）的「**关键角色矩阵**」：六到位全流程 19 个角色中 **10 个进判定**——**必选**电信全占「6 应标签约统筹 / 7 软硬件采购决策 / 9 全流程交付管理」（**+16 设备管理，仅涉硬件时必选**）；**三组二选一**各占一个：方案{3 设计者|4 整合确定者}、交付实施{10 设计者|11 确定责任者}、实施开发{13 实施者|14 主导责任者}。服务场景复用 `service_delivery_mode` 以及规划、资源、技术、管控、资质、交付责任等证据。角色可全部不勾，但必须用 `six_daowei_facts_confirmed=true` 明确表示“已经核对、未勾即未到位”，从而区分“确认没有”和“尚未填写”；相关事实变化后应重新确认。系统把既有答案映射为六个 `six_daowei_*` 字段的「到位 / 不到位 / 待补证据 / 不适用」建议，用户逐项确认；系统再给 `six_daowei_level=strong|medium|none` 建议，最终仍以右侧人工确认值为准。建议与人工结论不一致时只提示复核证据，不阻止诊断、不自动改写。

维度值 `not_applicable`（不适用）只向「采购自主」和「运维自主」开放，并且用户须确认目标核算单元的合同范围确实不存在对应活动、责任边界已经明确。单元完全不存在外部采购时，采购自主可以不适用；一次性设备单元完全不含质保、售后或运维责任时，运维自主可以不适用。客情掌握、方案总控、谈判应标自主、项目强管理是拟全额单元的核心主控证据，不能选择不适用。合法的不适用视为该维度已满足，不妨碍综合建议为强；系统自动把「留存合同责任边界等证明材料」加入标准证据清单，但不要求用户上传附件或填写说明。

**证据清单边界**：六到位、R08、白名单及其他未决条件只生成标准化补证清单，告诉用户送审前需要补齐或留存哪些材料。工具不提供附件上传，不要求用户录入补证说明，也不追踪材料是否已经补齐；它止于提醒，正式材料核验仍在线下送审流程完成。

**材料主目录（2026-07-19）**：以 2026-07-18 更新的《附件2 ICT项目全流程控制权角色职责和佐证材料》重建 `backend/rules/materials.json`。附件2是对六到位中「谁在什么环节主导、用什么留痕证明」的补充，不改变角色矩阵、PO1–PO4、R08、27号文公式、列收结果或风险算法。规则仅引用 `material_id`，报告仅在统一清单展开材料组成，并按「基础过程材料 / 条件性合规材料 / 财务列收材料 / 异常补正材料」四类展示。

**附件2角色证据强度**：角色 `1/3/4/6/7/9/10/11/13/14/16/18` 有独立核心佐证材料包。其中 `3/4/6/7/9/10/11/13/14/16` 进 10 角色判定矩阵，`1/18` 不进矩阵但分别为客情掌握、运维自主提供核心证据。角色 `2/5/8/12/15/17/19` 是配合角色，新附件未要求其独立主控佐证包；会议协调、任命书、人员名单、考勤和现场照片只能作为辅助材料，不能单独证明主控权。

**材料合并原则**：同一对象、同一目的的重复名称并入同一编号，例如方案迭代、项目管理、三流一致和付款条款。「控制权证明材料」「六到位关键角色及职责分工留痕」不再作为独立材料。但不同核查对象或不同审批事项不得因同名误合并：项目合作方与购买标书企业的特定标签查询分开，预付款与垫资的一事一议审批分开。
**辅助工具边界**：本产品只生成自检诊断报告，不实现「送省审核」按钮、送审状态或正式审批流，也不替代审核人的最终判断。报告可以包含暂按全额测算的高风险单元，并明确送审前必补清单；页面和报告不得使用「已送审」「审核通过」等会让用户误以为系统具有审批效力的表述。

“强”建议要求目标核算单元六维全部到位或有依据地不适用、关键角色齐备且不存在全部外部交付信号；“无”不得由“全部外包”单字段推出，必须同时有无自有能力、关键角色缺失及项目管理/运维核心维度不到位等多项事实；其余建议为“中”。**全额列收前置闸门按最终核算单元生效**：该单元满足「基础事实已核对 + 六维人工确认均为到位或合法不适用 + 关键角色矩阵成立 + 综合人工确认为强」时确认通过；任一适用维度明确不到位或综合结论明确为中/无时，确认不通过并落净额；选择待补证据或资格尚未确认时，不视为明确失败，自检仍按拟全额测算，但标为暂定、高风险并列出送审前必补项。任何结果只影响该单元，不连带其他独立核算单元。六到位确认通过不等于已经取得全额结论，仍需继续经过该单元的 R08 控制权和其他全额条件。标品始终全额，不受此 ICT 项目主控闸门改写。设计与实现见 `docs/adr/0003`；会计判断边界由 `docs/adr/0004` 收紧。
_Avoid_: 「7 个角色」——不严谨简写（单一情形占 6-7 个，但参选角色共 10 个，字段应给全 10 个）；把 19 角色全纳入判定（另 9 个为配合/客情/售后角色，不参选）。
_Avoid_: 询问或展示「整个项目是否六到位」；要求净额单元填写六到位；把某个单元的六到位失败扩散到其他单元。

**六到位（统一业务术语）**:
项目全流程管控与服务能力举证使用同一个名称、同一个模块「六到位」。「六必要」是历史误称，已废止，不得再作为另一套模型、模块或报告标题出现。服务场景沿用的规划、资源、技术、管控、资质、交付责任证据是六到位的细化证据；历史 `service_capability_level` 和项目级 `six_daowei_level` 只作旧记录读取兼容，新诊断逐个拟全额核算单元形成六维检查结论，严禁重新按 `service_delivery_mode` 单字段机械映射。
_Avoid_: 同时展示「六到位」和「六必要」让用户误以为需要完成两套检查；仅为术语统一而迁移历史枚举或数据库字段。

**履约义务控制权判断（会计结论）**:
位于核算单元确认和内部组合/分别关系之后，并复用项目级角色自查及 C1-C6 会计要件证据。组合核算单元整体判断一次，不对组内原始单元重复；独立核算单元分别判断，重点补充该项的定价权、转让前存货或履约风险、主要责任、验收及质保责任等差异证据。某一最终核算单元不具备控制权，只影响该项落净额，不连带其他核算单元。交互上共性答案自动复用到各项，用户只确认例外或差异。
**控制权事实 `CTRL1–CTRL4`**：每个需要决定总额/净额的独立履约义务只确认四项是/否事实：`CTRL1` 电信是否在转让客户前取得该项控制权；`CTRL2` 电信是否对该项质量、验收、售后承担主要责任；`CTRL3` 电信是否承担该项存货、交付或返工风险；`CTRL4` 电信是否拥有该项自主定价权。重大整合、方案主导、采购及交付角色等已在履约义务拆分或项目级角色自查中采集，不得逐项重复询问。`CTRL1–CTRL4` 位于 `PO1–PO4` 派生分支之后，不能反向改变 `combined|separate`。
**R08 归位**：`CTRL1–CTRL4` 不新增顶层模块，而是现有 R08「控制权六维度」的去重、类型化展示。原维度（三）重大整合已由 `PO2` 承接；原维度（一）取得商品控制权与（二）主导第三方服务按履约义务类型落入 `CTRL1`（设备/成品软件问转让前商品控制，施工问是否实质主导第三方履约）；原维度（四）主要责任、（五）存货/履约风险、（六）定价权依次落入 `CTRL2–CTRL4`。组合履约在现有「控制权自查」中只展示一份整体 R08；分别履约才在对应核算单元下展开精简 R08。
**提交规则**：四项未作答属于表单未完成，可阻止提交；答案之间证据支撑较弱或看似矛盾时，只提示补充举证并计入风险，不阻止诊断、不自动改写用户确认的控制权结论。正式送审时由审核人结合证据下结论。
_Avoid_: 把项目级角色矩阵直接当作所有单元共用的一票否决结论；在 `combined` 分支仍逐单元重复询问控制权；在 `separate` 分支重复询问已经由项目级角色自查确认的共性事实。

**关键角色 / 配合角色**:
进总额法判定矩阵的 10 个为「**关键角色**」（决策 / 主导 / 责任）。附件2另对角色 1（商机挖掘与客情统筹）和 18（运维履约责任）规定了核心佐证：两者不进 10 角色矩阵，但也不得简化成「只是配合」。只有角色 `2/5/8/12/15/17/19` 按新附件定位为无独立主控佐证包的「**配合角色**」；电信仅占这些配合角色不能证明控制权，可形成过手嫌疑。

**敏感自由文本**:
用户在对话中输入的原文。准标识符四要素只活在这里，不进结构化字段（唯一例外是 `bpm_id`，可正则脱敏）。脱敏的真正目标对象就是这段文本。

**内网版 / 公网版**:
两个平行部署变体。内网版数据不出公司（含本地大模型替代 DeepSeek），功能完整、含跨人复核；公网版受脱敏边界约束、强制前端脱敏、砍掉跨人复核、退化为「前线人员单人自助诊断」。
_Avoid_: 把两者做成两份 fork 代码——应是同一套代码 + `DEPLOYMENT_MODE` 开关。

**脱敏边界 (ii)+(iii)**:
(ii) 明文 PII 不得落在任何公网可达服务器上（含自有服务器）；(iii) 全程 HTTPS。第三方 DeepSeek 这一跳同样在约束内。

## Relationships

- 诊断引擎只依赖**枚举字段**，与**准标识符四要素**正交 → 脱敏四要素不损伤诊断结论（已由 `rules.json` + 字段定义证实）。
- **毛利率（应列收/服务侧）** 与**列收**同源：都只对服务单元有意义 → 毛利驱动的规则（R03/R12/R32）本质上是在问「服务侧列收风险」，扁平 `gross_margin` 必须只承载服务侧毛利。
- **公网版** 受 **脱敏边界 (ii)+(iii)** 约束 → 倒逼「前端脱敏」（方案 A）。
- 客户端脱敏对**自由文本**只能「尽力」（NER 非 100%）；只有**确定性手段**（专属输入框 + 正则）能提供「保证不漏」。
- **六到位自查**与**履约义务控制权判断**是「项目共性事实复用 + 单元全额前置闸门 → 单元会计结论」关系：角色、交付模式等共性事实只采一次；组合核算单元确认一次六到位，分别履约的目标单元分别确认。某单元未通过只使该单元净额；通过后再对同一单元完成 R08。两者有证据重合，但不得重复采集。
- **六到位自查**与**硬转服务**仍分层互补：前者记录项目全流程主导与服务履约证据，后者检查某个申报服务单元是否呈现硬件/施工实质；它们不互相替代。
- **项目整体风险**按所有规则和单元风险取最高值，但**列收结果仍逐单元独立**：高风险单元可以把项目摘要抬为高风险，不得连带把其他合格单元改成净额。
- 完整链条为：**核算单元确认**（结构事实）→ **内部组合/分别关系**（页面仍统一称核算单元）→ **项目共性证据复用 + 目标核算单元六到位闸门**（未过只使该单元净额）→ **该单元 R08 控制权判断** → **按整个 BPM 项目取数的省内配套门槛**（产数结果）→ **差口举证**。工具只走到「分类 + 举证」，**不**下财税科目。
- **项目整体利润率** 与 **服务侧毛利率** 同源不同用：前者含硬件、喂模式门槛；后者只服务侧、喂三零/过手。两者切点共用同套分桶枚举，但**严禁互串**。

## Flagged ambiguities

- 「脱敏边界」最初被理解为「浏览器→服务器」单跳；澄清后实为 (ii)+(iii)，且 DeepSeek 第三方跳同样在内——现状（内网调外部 DeepSeek）其实已不合规，靠「测试阶段」容忍。
- 「风险」最初按逐字段（单条泄漏即违规）理解；澄清后为**组合可重识别性**——准标识符四要素凑齐才算泄密，单字段可控。
- 「前端脱敏」最初设想为浏览器自动识别 PII；澄清后，自由文本里的机构名/项目名无法靠 NER 保证，需用**专属字段**做确定性脱敏。
- 用户口语「项目分三层」混淆了两类东西：设备/服务是可分别核算的**核算单元**，而「自有能力」是跨单元的**属性**（对应 `has_telecom_capability`），不是一层。canonical 概念定为**核算单元**，「列收单元」因预设列收被否决。
- **毛利率最初被当作「项目整体毛利率」**（单一扁平值喂 R03/R12/R32）；澄清后实为「应列收/服务侧毛利」——列收已按核算单元免疫硬件/施工，毛利这条线却没跟上。现状修法（2026-06-06）：(i) 收紧扁平字段语义为「服务侧」+ AI 抽取提示禁止混入硬件毛利（已做）；(ii) 规则按服务单元逐块跑毛利（**已知缺口**，需引擎支持逐单元规则迭代，类比硬转服务检测，未实现）。
- **2026-07 准则化重构已实现**（决策落 `docs/adr/0004`）：新会话保存 `accounting_units_json.schema_version=2`，拆开标品与成品软件，以 PO1–PO4 确认组合关系，并用 `listing_intent`、`listing_result`、`listing_result_status` 分开表达意图、金额口径和暂定状态；旧数组及 `listed` 路径只保留历史报告兼容。
**两个 60% 别混**（grill 中澄清）：①**服务整合**「设备+成品软件+集成施工金额占比≤60%」（第 11 页，资格闸·上限，进占比公式）②**分别履约的原单一履约场景**「集采比例≥60%」（第 12 页，后向采购里走集采框架协议的设备占比，**软建议**·下限，且「省采购是否授权市采购待定」→ 单列字段、缺则黄字提示不一票否决）。
**本轮领域 grill 已收口**：当前产品只输出带 `confirmed|provisional` 状态的自检报告，不建设正式送审动作；后续进入决策账本、代码和测试的双向核对。

## Decision capture

- Grill 过程中每个已确认领域结论必须当场写入本文件；涉及难以逆转的结构取舍时同步写入 `docs/adr/`，不得只依赖聊天记忆。
- 实施前按「对话确认 → `CONTEXT.md` → ADR → `AGENTS.md`/`CLAUDE.md` → 代码与测试」做一次双向核对；代码仍停留在旧模型时必须明确标注为待重构，不得用现状覆盖已确认目标。
